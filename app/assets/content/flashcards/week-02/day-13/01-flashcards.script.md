# Audio script — Flashcards — DSA: Stack / Queue / Linked List
> Listen-only flashcard Q&A from `week-02.md`. Spoken answers.

## §0 Q1. Array as queue

Next. Array as queue? Answer. removeFirst() is O(n) — call it out or use two-stack / Deque.

## §1 Q2. Two-stack queue

Next. Two-stack queue? Answer. Amortized O(1) — not strict O(1) every dequeue.

## §2 Q3. Linked list in Swift apps

Next. Linked list in Swift apps? Answer. Interview skill + LRU design — not everyday UITableView.

## §3 Q4. Agenda-first

Next. Agenda-first? Answer. 2–3 min spoken plan before typing.

## §4 Q5. BookMyShow SSL pinning + URLSession migration bridge

Next. BookMyShow SSL pinning + URLSession migration bridge? Answer. Refresh waiters = FIFO queue of continuations (soft, not shipped LL).

## §5 Q6. Forbidden

Next. Forbidden? Answer. “We used linked lists for the feed”.
