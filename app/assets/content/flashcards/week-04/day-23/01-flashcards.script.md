# Audio script — Flashcards — DSA HashMap / Heap + Mixed Unknown-Pattern
> Listen-only flashcard Q&A from `week-04.md`. Spoken answers.

## §0 Q1. Hash

Next. Hash? Answer. Expected O(1) lookup — design a good key.

## §1 Q2. Heap Top-K

Next. Heap Top-K? Answer. Min-heap size K for Kth largest — root is answer.

## §2 Q3. Negatives + subarray sum

Next. Negatives + subarray sum? Answer. Prefix+hash — not sliding window.

## §3 Q4. Unknown pattern

Next. Unknown pattern? Answer. 90s classify before coding.

## §4 Q5. Average vs worst hash

Next. Average vs worst hash? Answer. Say expected O(n); pathological collisions exist.

## §5 Q6. Production

Next. Production? Answer. Maps for coalescing — not “search is Two Sum”.
