# Audio script — Revision guide — DSA: Stack · Queue · Linked List
> Listen-only revision day guide from `day-13.md`.

## §0 1. Outcome

Next. 1. Outcome. Explain aloud, in plain sentences: Stack (LIFO) and queue (FIFO) — complexity and Swift footguns Monotonic stack, valid parentheses, min stack, two-stack queue Linked list classics: reverse, Floyd cycle, merge — with Swift honesty.

## §1 2. Concept refresh (simple)

Next. 2. Concept refresh (simple). 2. Concept refresh (simple).

## §2 2.1 Stack & queue

Next. 2.1 Stack & queue. Navigation back stack is LIFO. Single-flight refresh waiters are FIFO continuations (soft BookMyShow SSL pinning + URLSession migration bridge).

## §3 2.2 Monotonic stack patterns

Next. 2.2 Monotonic stack patterns. Valid parentheses, daily temperatures, next greater element — scan once, push/pop indices or values. State monotonic property gives O(n).

## §4 2.3 Linked lists in Swift apps

Next. 2.3 Linked lists in Swift apps. Interview skill + LRU design vocabulary — not everyday UITableView implementation. Prefer Array for locality and ergonomics in production U I code.

## §5 2.4 Critical truths (pin)

Next. 2.4 Critical truths (pin). 2.4 Critical truths (pin).

## §6 3. Read these

Next. 3. Read these. Suggested sample order: 01-stack-queue-basics → 02-monotonic-patterns → 03-linked-list-algos → 04-production-bridges.

## §7 4. Map to your work

Next. 4. Map to your work. Soft bridge · BookMyShow SSL pinning + URLSession migration mindset: Token refresh waiters queue behind one in-flight refresh — FIFO discipline, not a shipped linked-list module. Soft bridge · BookMyShow LE Bottom Sheet: Nav is LIFO; LE Bottom Sheet reduced full-screen pushes — verified 30%+ fewer full-screen navigations. Soft bridge · Hybrid U I / deeplinks: One router; stack discipline in hybrid nav. Interview line (≤20s): “For coding I state structure and complexity first — monotonic stack O(n) — then write clean iterative Swift; linked lists are interview skill, not my UITableView default.”.

## §8 5. Flash prompts

Next. 5. Flash prompts. 1. Stack vs queue — one example each + complexity 2. Why Array.removeFirst() is a footgun 3. Two-stack queue — amortized vs strict O(1) 4. Monotonic stack — when and why O(n).

## §9 6. Timed drills

Next. 6. Timed drills. Expand from sample cards and 07-revision-qna answer points. Pick Ads or S D U I track tonight for Day 14.
