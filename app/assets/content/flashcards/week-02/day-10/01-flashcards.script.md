# Audio script — Flashcards — SDUI / CMS · Schema Versioning · Fallbacks
> Listen-only flashcard Q&A from `week-02.md`. Spoken answers.

## §0 Q1. SDUI def

Next. S D U I def? Answer. Schema → version gate → registry → native render.

## §1 Q2. Not SDUI

Next. Not S D U I? Answer. JS eval, whole-app WebView, arbitrary code in JSON.

## §2 Q3. Unknown component

Next. Unknown component? Answer. Skip + metric — never crash.

## §3 Q4. Empty root

Next. Empty root? Answer. Hard fallback — blank chrome unacceptable.

## §4 Q5. BookMyShow backend-driven header & search verified

Next. BookMyShow backend-driven header & search verified? Answer. Book My Show backend-driven / protocolised header.

## §5 Q6. BookMyShow backend-driven header & search-A1

Next. BookMyShow backend-driven header & search-A1? Answer. Schema versioning + unknown fallback = design.

## §6 Q7. Audio streaming + server-driven splash (Aces) verified

Next. Audio streaming + server-driven splash (Aces) verified? Answer. Aces server-driven splash — no invented ms.

## §7 Q8. Ads media

Next. Ads media? Answer. BookMyShow Ads pipeline + HeroWidget lifecycle native lifecycle — config maybe S D U I, video stays native.
